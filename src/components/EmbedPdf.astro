---
interface Props {
  src: string;
  title?: string;
  height?: string;
}

const {
  src,
  title = '埋め込みPDF',
  height = '700px'
} = Astro.props;
---

<div class="pdf-container">
  <iframe
    src={src}
    style={`width:100%; height:${height}; border:0;`}
    title={title}
  />
  <p>
    お使いのブラウザはPDFの埋め込み表示に対応していません。
    <a href={src}>こちらからダウンロード</a>してください。
  </p>
</div>

<style>
  .pdf-container {
    width: 100%;
    max-width: 800px;
    margin: 2em auto;
    border: 1px solid #ccc;
  }
</style>
